#include <stdio.h>
#include <stdlib.h>

    //2. Faça um algoritmo para calcular quantas ferraduras são necessárias para equipar todos os cavalos comprados para um haras.

int main()
{
    int cavalos, ferraduras;

    printf("Digite a quantidade de cavalos: ");
    scanf("%d", &cavalos);
    printf("A quantidade de ferraduras e: %d unidades", 4*cavalos);

    return 0;
}
